type: object
properties:
  user_name:
    type: string
    description: New user name (optional)
    example: "Jane Smith"
  currency_code:
    type: string
    description: User's preferred currency code (ISO 4217 format, optional)
    pattern: '^[A-Z]{3}$'
    example: "USD"
