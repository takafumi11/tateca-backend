type: object
required:
  - from
  - to
  - amount
properties:
  from:
    $ref: './UserResponse.yaml'
  to:
    $ref: './UserResponse.yaml'
  amount:
    type: integer
    format: int64
    description: Settlement amount
    example: 1500